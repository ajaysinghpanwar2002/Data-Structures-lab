#include<bits/stdc++.h>
using namespace std;

struct node {
    int data ;
    node *next ;
};

class stack{
    node*top;
    public:
    stack()
    {
        top = NULL;
    }
    node * create(int x)
    {
        node *ptr;
        // ptr = new node;
        // ptr => data = x;
        // ptr => next = null;
        // return ptr;
    }
};